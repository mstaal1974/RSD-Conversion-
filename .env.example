# Copy to .env and fill in values — never commit .env to git

# ---------------------------------------------------------------------------
# LLM — set at least one
# ---------------------------------------------------------------------------
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_MODEL=gpt-4.1-mini

# ---------------------------------------------------------------------------
# Database (Postgres — leave blank for session-only mode)
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql+psycopg://user:password@host:5432/rsd

# ---------------------------------------------------------------------------
# TGA (training.gov.au) Web Services
# ---------------------------------------------------------------------------
# Sandbox read-only credentials — safe for development and testing
TGA_USERNAME=WebService.Read
TGA_PASSWORD=Asdf098
TGA_ENV=sandbox        # sandbox | production

# For production access email: tgaproject@education.gov.au
# Production credentials are unique per organisation
